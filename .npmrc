# pnpm workspace configuration.
# Peer deps auto-installed so React/Electron peer graphs resolve without manual pins.
auto-install-peers=true
# Do not fail the whole install on unmet peer ranges during early scaffolding.
strict-peer-dependencies=false
# Keep the store link mode deterministic across the OS-matrix runners.
prefer-workspace-packages=true
