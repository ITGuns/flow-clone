// Formatting service surface (apps/api). HaikuFormatter is the real §2.2 Formatter; the
// prompt builder and stream-factory seam are exported for tests and the WS gateway wiring.
export * from './prompt';
export * from './haiku-formatter';
