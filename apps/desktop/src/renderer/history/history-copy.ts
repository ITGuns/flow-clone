// User-facing constants for the history view, kept in one place so tests and components agree and a
// future localization pass has a single seam. No transcript content ever lives here.

/** The word the user must type to arm the destructive "clear all history" action. */
export const CLEAR_ALL_CONFIRM_WORD = 'DELETE';

/** Placeholder for the search box — states the exact-word semantics up front (task spec). */
export const SEARCH_PLACEHOLDER = 'Search words…';
