{
  "name": "@undertone/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Undertone contracts as code: domain types, provider interfaces, WS protocol, frame codec, error taxonomy. The ONLY place protocol/domain types live.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run"
  }
}
